import 'package:flutter/material.dart';
import 'package:web_testing/common/colors.dart';

class TabletUserInfo extends StatelessWidget {
  const TabletUserInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(borderRadius: BorderRadius.circular(16), color: myGrey[700]),
    );
  }
}
